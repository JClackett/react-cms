type Block {
	id: ID!
	type: String!
	content: String
}

type Mutation {
	createBlock(type: String!, content: String, collection: String!): Block
	updateBlock(id: String!, content: String!): Boolean
}
