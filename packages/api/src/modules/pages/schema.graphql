type Page {
	id: ID!
	name: String!
	slug: String!
	collections: [Collection]
}

type Query {
	listPages: [Page]
	findPage(id: String!): Page
}

type Mutation {
	createPage(name: String!, slug: String!): Page
}
